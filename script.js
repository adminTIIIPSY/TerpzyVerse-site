function githubLogin() {
  alert('GitHub login will be connected soon.');
}
function resetPassword() {
  alert('Password reset function coming soon.');
}
document.getElementById('loginForm')?.addEventListener('submit', function(e) {
  e.preventDefault();
  alert('Login functionality will be added with Firebase Auth.');
});
document.getElementById('registerForm')?.addEventListener('submit', function(e) {
  e.preventDefault();
  alert('Register functionality will be added with Firebase Auth.');
});
